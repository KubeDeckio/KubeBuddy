# Example Reports

These files are illustrative report samples for documentation links. They are not regenerated on every check-catalog change, so the exact check totals and newest check IDs may differ from the current runtime output.
